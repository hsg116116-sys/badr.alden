# 🚨 حل مشكلة: الطلبات لا تظهر للجزار

## المشكلة:
- العميل يطلب طلب جديد ✅
- الطلب **لا يظهر** في صفحة الجزار ❌

## السبب:
مشكلة في **Row Level Security (RLS)** - الجزار ليس لديه صلاحية لرؤية الطلبات!

---

## ✅ الحل (دقيقتين):

### الخطوة 1: افتح Supabase
https://supabase.com → مشروعك → **SQL Editor**

### الخطوة 2: نفّذ السكريبت
1. افتح الملف: **`FIX_BUTCHER_VIEW_ORDERS.sql`**
2. انسخ محتواه **بالكامل**
3. الصقه في Supabase SQL Editor  
4. اضغط **Run** (أو Ctrl+Enter)

### الخطوة 3: تحقق
بعد التنفيذ، نفّذ هذا الاستعلام للتحقق:

```sql
-- تحقق من وجود طلبات
SELECT id, status, customer_name, created_at 
FROM orders 
WHERE status = 'pending' 
ORDER BY created_at DESC;
```

**يجب أن ترى الطلبات الموجودة!**

### الخطوة 4: حدّث صفحة الجزار
- اضغط **F5** أو **Ctrl+R** لتحديث الصفحة
- يجب أن تظهر الطلبات الآن! 🎉

---

## 🧪 اختبار سريع:

### 1. أنشئ طلب تجريبي من حساب عميل
- سجل دخول كعميل
- أضف منتجات للسلة
- اطلب الطلب

### 2. سجل دخول كجزار
- افتح صفحة "محطة التجهيز والتقطيع"
- **يجب أن تظهر** الطلب الجديد فوراً!

---

## 🔧 إذا لم يعمل:

### 1. تأكد من وجود حساب جزار
```sql
SELECT id, username, role FROM users WHERE role = 'butcher';
```

### 2. تأكد من أن الجزار غير محظور
```sql
SELECT username, role, is_banned FROM users WHERE role = 'butcher';
```

### 3. تحقق من الصلاحيات
```sql
SELECT tablename, policyname 
FROM pg_policies 
WHERE tablename = 'orders';
```

---

## 📋 ملخص ما يفعله السكريبت:

1. ✅ يحذف السياسات القديمة المقيّدة
2. ✅ ينشئ سياسات جديدة تسمح للجزارين برؤية **جميع** الطلبات
3. ✅ يسمح للجزارين بتحديث حالة الطلبات
4. ✅ يسمح للجزارين برؤية تفاصيل الطلبات (order items)

---

## ✨ النتيجة المتوقعة:

بعد تنفيذ السكريبت:
- ✅ الجزار يستطيع رؤية **جميع** الطلبات الجديدة
- ✅ الجزار يستطيع استلام الطلبات
- ✅ الجزار يستطيع تحديث حالة الطلبات
- ✅ النظام يعمل بشكل كامل! 🚀
